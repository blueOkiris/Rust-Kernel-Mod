[package]
name = "hello_rust"
version = "0.1.0"
edition = "2018"

[lib]
name = "hello_rust"
crate_type = [ "staticlib" ]

[profile.dev]
panic = "abort"
overflow-checks = false

[profile.release]
panic = "abort"
overflow-checks = false

[dependencies]
